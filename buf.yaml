version: v2

modules:
  - path: api/protos
    name: buf.build/servora/servora

deps:
  - buf.build/bufbuild/protovalidate

breaking:
  use:
    - FILE

lint:
  use:
    - STANDARD
  except:
    - ENUM_VALUE_PREFIX
    - ENUM_ZERO_VALUE_SUFFIX
    - RPC_REQUEST_RESPONSE_UNIQUE
