// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable no-use-before-define, @typescript-eslint/no-unused-vars, prettier/prettier */

interface TransportMeta {
  service: string;
  method: string;
}

export interface ClientTransport {
  unary<T>(
    path: string,
    method: string,
    body: null | string,
    meta: TransportMeta,
  ): Promise<T>;
  serverStream<T>(path: string, meta: TransportMeta): ServerStream<T>;
  duplexStream<TIn, TOut>(path: string, meta: TransportMeta): DuplexStream<TIn, TOut>;
}

export interface ServerStream<T> {
  onEvent(listener: (data: T) => void): () => void;
  onError(handler: (error: Error) => void): void;
  close(): void;
}

export interface DuplexStream<TIn, TOut> extends ServerStream<TOut> {
  send(data: TIn): void;
}

function encodePathSegment(value: unknown): string {
  return encodeURIComponent(String(value)).replace(/[!'()*]/g, (character) => `%${character.charCodeAt(0).toString(16).toUpperCase()}`);
}

function encodeMultiSegmentPath(value: unknown): string {
  return String(value)
    .split('/')
    .map(encodePathSegment)
    .join('/');
}

// User 表示 tenant 下的公开用户资源。
export type User = {
  // create_time 是服务端记录的创建时间。
  //
  // Behaviors: OUTPUT_ONLY
  createTime?: wellKnownTimestamp;
  // delete_time 是软删除发生时间；未删除时为空。
  //
  // Behaviors: OUTPUT_ONLY
  deleteTime?: wellKnownTimestamp;
  // display_name 是调用方可修改的显示名称；显式 Clear 写入空字符串。
  //
  // Behaviors: OPTIONAL
  displayName?: string;
  // email 是创建 User 时必须提供且非空的电子邮箱。
  //
  // Behaviors: REQUIRED
  email?: string;
  // etag 是服务端并发控制值；生命周期由业务显式处理。
  etag?: string;
  // name 是 User 的 canonical AIP 资源名。
  //
  // Behaviors: IDENTIFIER
  name?: string;
  // nickname 保留 presence；显式 Clear 写入 NULL。
  //
  // Behaviors: OPTIONAL
  nickname?: string;
  // purge_time 是计划物理清除时间；本示例无限期保留，因此可一直为空。
  //
  // Behaviors: OUTPUT_ONLY
  purgeTime?: wellKnownTimestamp;
  // temporary_password 只供 Create/Update 输入；存储层转换为私有 password_hash。
  //
  // Behaviors: OPTIONAL, INPUT_ONLY
  temporaryPassword?: string;
  // tenant_plan 是创建后不可修改的租户套餐快照。
  //
  // Behaviors: OPTIONAL, IMMUTABLE
  tenantPlan?: string;
  // update_time 是服务端记录的最近更新时间。
  //
  // Behaviors: OUTPUT_ONLY
  updateTime?: wellKnownTimestamp;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

// GetUserRequest 按 canonical 资源名定位 User。
export type GetUserRequest = {
  // name 是待获取 User 的 canonical 资源名。
  //
  // Behaviors: REQUIRED
  name?: string;
};

// ListUsersRequest 描述一个标准 User 集合查询。
export type ListUsersRequest = {
  // filter 是 AIP-160 确定性子集表达式。
  //
  // Behaviors: OPTIONAL
  filter?: string;
  // 只有 true 时执行 Count 并返回 total_size。
  // Servora 的可选 total 扩展；项目 api-linter 配置需允许该附加字段。
  // (-- api-linter: core::0132::request-unknown-fields=disabled --)
  //
  // Behaviors: OPTIONAL
  includeTotal?: boolean;
  // order_by 是 AIP-132 排序表达式。
  //
  // Behaviors: OPTIONAL
  orderBy?: string;
  // page_size 是服务端最多返回的资源数量。
  //
  // Behaviors: OPTIONAL
  pageSize?: number;
  // page_token 是前一页返回的不透明连续翻页令牌。
  //
  // Behaviors: OPTIONAL
  pageToken?: string;
  // parent 是待查询 User collection 的 tenant 资源名。
  //
  // Behaviors: REQUIRED
  parent?: string;
  // true 时显式包含正常资源与 tombstone；默认只返回正常资源。
  //
  // Behaviors: OPTIONAL
  showDeleted?: boolean;
  // 可单独使用，也可在 page_token 恢复 cursor 后额外跳过资源；服务可以限制最大值。
  // Servora 的随机跳页扩展；项目 api-linter 配置明示允许该字段。
  // (-- api-linter: core::0158::request-skip-field=disabled --)
  //
  // Behaviors: OPTIONAL
  skip?: string;
};

// ListUsersResponse 包含一页 User 及分页元数据。
export type ListUsersResponse = {
  // next_page_token 是下一页的不透明连续翻页令牌；末页为空。
  nextPageToken?: string;
  // 仅在 include_total 为 true 时设置。Servora 明示使用 optional int64
  // 表达“未计算”并支持大型集合。
  // Servora 的可选 total 扩展；项目 api-linter 配置明示允许该字段。
  // (-- api-linter: core::0132::response-unknown-fields=disabled --)
  totalSize?: string;
  // users 是当前页返回的 User 列表。
  users?: User[];
};

// CreateUserRequest 描述创建 User 所需输入。
export type CreateUserRequest = {
  // parent 是新 User 所属 tenant 的 canonical 资源名。
  //
  // Behaviors: REQUIRED
  parent?: string;
  // user 是待创建的 User；请求中的 name 被忽略。
  //
  // Behaviors: REQUIRED
  user?: User;
  // 成为 User.name 的末段；请求中的 user.name 被忽略。
  //
  // Behaviors: REQUIRED
  userId?: string;
};

// UpdateUserRequest 描述 User 更新及其字段选择。
export type UpdateUserRequest = {
  // allow_missing 允许业务按 AIP-134 执行 upsert。
  //
  // Behaviors: OPTIONAL
  allowMissing?: boolean;
  // update_mask 选择待更新字段；省略时按标准隐式 mask 处理。
  //
  // Behaviors: OPTIONAL
  updateMask?: wellKnownFieldMask;
  // user.name 定位待更新资源。
  //
  // Behaviors: REQUIRED
  user?: User;
};

// In JSON, a field mask is encoded as a single string where paths are
// separated by a comma. Fields name in each path are converted
// to/from lower-camel naming conventions.
// As an example, consider the following message declarations:
//
//     message Profile {
//       User user = 1;
//       Photo photo = 2;
//     }
//     message User {
//       string display_name = 1;
//       string address = 2;
//     }
//
// In proto a field mask for `Profile` may look as such:
//
//     mask {
//       paths: "user.display_name"
//       paths: "photo"
//     }
//
// In JSON, the same mask is represented as below:
//
//     {
//       mask: "user.displayName,photo"
//     }
type wellKnownFieldMask = string;

// DeleteUserRequest 按 canonical 资源名定位待删除 User。
export type DeleteUserRequest = {
  // allow_missing 为 true 时删除不存在资源可由业务返回幂等结果。
  //
  // Behaviors: OPTIONAL
  allowMissing?: boolean;
  // etag 非空时要求与当前资源一致。
  //
  // Behaviors: OPTIONAL
  etag?: string;
  // name 是待删除 User 的 canonical 资源名。
  //
  // Behaviors: REQUIRED
  name?: string;
};

// UndeleteUserRequest 按 canonical 资源名定位待恢复 User。
export type UndeleteUserRequest = {
  // name 是待恢复 User 的 canonical 资源名。
  //
  // Behaviors: REQUIRED
  name?: string;
};

// UserService 提供 User 资源的标准 CRUD 方法及 AIP-164 恢复方法。
export interface UserService {
  // GetUser 根据资源名取得一个 User；授权后的实现可显式读取 tombstone。
  GetUser(
    request: GetUserRequest,
  ): Promise<User>;
  // ListUsers 列出指定 tenant 下的 User。
  ListUsers(
    request: ListUsersRequest,
  ): Promise<ListUsersResponse>;
  // CreateUser 在指定 tenant 下创建一个 User。
  CreateUser(
    request: CreateUserRequest,
  ): Promise<User>;
  // UpdateUser 更新一个已有 User。
  UpdateUser(
    request: UpdateUserRequest,
  ): Promise<User>;
  // DeleteUser 软删除 User 并返回更新后的 tombstone。
  DeleteUser(
    request: DeleteUserRequest,
  ): Promise<User>;
  // UndeleteUser 恢复一个已软删除的 User。
  UndeleteUser(
    request: UndeleteUserRequest,
  ): Promise<User>;
}

export function createUserServiceClient(
  transport: ClientTransport,
): UserService {
  return {
    GetUser(request) {
      if (request.name === undefined || request.name === null || request.name === '') {
        throw new Error('missing required field request.name');
      }
      const path = `v1/${encodeMultiSegmentPath(request.name)}`;
      const body = null;
      return transport.unary<User>(path, 'GET', body, {
        service: 'UserService',
        method: 'GetUser',
      });
    },
    ListUsers(request) {
      if (request.parent === undefined || request.parent === null || request.parent === '') {
        throw new Error('missing required field request.parent');
      }
      const path = `v1/${encodeMultiSegmentPath(request.parent)}/users`;
      const body = null;
      const queryParams: string[] = [];
      if (request.pageSize !== undefined && request.pageSize !== null) {
        queryParams.push(
          `pageSize=${encodeURIComponent(request.pageSize.toString())}`,
        );
      }
      if (request.pageToken !== undefined && request.pageToken !== null) {
        queryParams.push(
          `pageToken=${encodeURIComponent(request.pageToken.toString())}`,
        );
      }
      if (request.skip !== undefined && request.skip !== null) {
        queryParams.push(
          `skip=${encodeURIComponent(request.skip.toString())}`,
        );
      }
      if (request.filter !== undefined && request.filter !== null) {
        queryParams.push(
          `filter=${encodeURIComponent(request.filter.toString())}`,
        );
      }
      if (request.orderBy !== undefined && request.orderBy !== null) {
        queryParams.push(
          `orderBy=${encodeURIComponent(request.orderBy.toString())}`,
        );
      }
      if (request.includeTotal !== undefined && request.includeTotal !== null) {
        queryParams.push(
          `includeTotal=${encodeURIComponent(request.includeTotal.toString())}`,
        );
      }
      if (request.showDeleted !== undefined && request.showDeleted !== null) {
        queryParams.push(
          `showDeleted=${encodeURIComponent(request.showDeleted.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary<ListUsersResponse>(uri, 'GET', body, {
        service: 'UserService',
        method: 'ListUsers',
      });
    },
    CreateUser(request) {
      if (request.userId === undefined || request.userId === null || request.userId === '') {
        throw new Error('missing required field request.user_id');
      }
      if (request.user === undefined || request.user === null) {
        throw new Error('missing required field request.user');
      }
      if (request.parent === undefined || request.parent === null || request.parent === '') {
        throw new Error('missing required field request.parent');
      }
      const path = `v1/${encodeMultiSegmentPath(request.parent)}/users`;
      const body = JSON.stringify(request?.user ?? {});
      const queryParams: string[] = [];
      if (request.userId !== undefined && request.userId !== null) {
        queryParams.push(
          `userId=${encodeURIComponent(request.userId.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary<User>(uri, 'POST', body, {
        service: 'UserService',
        method: 'CreateUser',
      });
    },
    UpdateUser(request) {
      if (request.user?.name === undefined || request.user?.name === null || request.user?.name === '') {
        throw new Error('missing required field request.user.name');
      }
      const path = `v1/${encodeMultiSegmentPath(request.user.name)}`;
      const body = JSON.stringify(request?.user ?? {});
      const queryParams: string[] = [];
      if (request.updateMask !== undefined && request.updateMask !== null) {
        queryParams.push(
          `updateMask=${encodeURIComponent(request.updateMask.toString())}`,
        );
      }
      if (request.allowMissing !== undefined && request.allowMissing !== null) {
        queryParams.push(
          `allowMissing=${encodeURIComponent(request.allowMissing.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary<User>(uri, 'PATCH', body, {
        service: 'UserService',
        method: 'UpdateUser',
      });
    },
    DeleteUser(request) {
      if (request.name === undefined || request.name === null || request.name === '') {
        throw new Error('missing required field request.name');
      }
      const path = `v1/${encodeMultiSegmentPath(request.name)}`;
      const body = null;
      const queryParams: string[] = [];
      if (request.etag !== undefined && request.etag !== null) {
        queryParams.push(
          `etag=${encodeURIComponent(request.etag.toString())}`,
        );
      }
      if (request.allowMissing !== undefined && request.allowMissing !== null) {
        queryParams.push(
          `allowMissing=${encodeURIComponent(request.allowMissing.toString())}`,
        );
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return transport.unary<User>(uri, 'DELETE', body, {
        service: 'UserService',
        method: 'DeleteUser',
      });
    },
    UndeleteUser(request) {
      if (request.name === undefined || request.name === null || request.name === '') {
        throw new Error('missing required field request.name');
      }
      const path = `v1/${encodeMultiSegmentPath(request.name)}:undelete`;
      const body = JSON.stringify(request);
      return transport.unary<User>(path, 'POST', body, {
        service: 'UserService',
        method: 'UndeleteUser',
      });
    },
  };
}
export class ApiClient {
  private readonly _transport: ClientTransport;
  private _userService?: UserService;

  constructor(transport: ClientTransport) {
    this._transport = transport;
  }

  get userService(): UserService {
    return this._userService ??= createUserServiceClient(this._transport);
  }
}

export function createApiClient(transport: ClientTransport): ApiClient {
  return new ApiClient(transport);
}


// @@protoc_insertion_point(typescript-http-eof)
